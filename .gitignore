# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Supabase CLI local cache/link state — not shared with the team
supabase/.temp/

# Claude Code local/personal settings — not shared with the team
.claude/settings.local.json

# Environment — never commit credentials
.env
.env.*
!.env.example

# Deno lockfile — byproduct of `npx deno@latest check` on the Edge
# Functions. There is no deno.json in this repo, so nothing reads it.
deno.lock
